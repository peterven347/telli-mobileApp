import React, { } from "react"
import { Text, View } from "react-native"

export default function GameScreen() {
    return (
        <View style={{ flex: 1 }}>
            <Text>Games</Text>
        </View>
    )
}